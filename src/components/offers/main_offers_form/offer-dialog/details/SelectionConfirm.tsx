import React from 'react';
import { useDetailsContext } from './DetailsContext';

const SelectionConfirm: React.FC = () => {
  // Always return null to disable the confirmation dialog
  return null;
};

export default SelectionConfirm; 