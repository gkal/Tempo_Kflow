/**
 * CustomerForm
 * 
 * This file is a proxy that re-exports the refactored CustomerForm component
 * to maintain backward compatibility with both default and named imports.
 */

import { CustomerForm } from './CustomerForm-refactored';

// Re-export as both named and default export
export { CustomerForm };
export default CustomerForm;

